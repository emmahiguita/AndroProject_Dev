# ============================================================================
# AndroProject Vision Backend - Dependencias
# Instalar: pip install -r requirements.txt
# CUDA (opcional): pip install -r requirements-cuda.txt
# ============================================================================

# WebRTC y servidor
aiortc>=1.9.0
aiohttp>=3.9.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Video y procesamiento
av>=12.0.0                 # PyAV - encoding/decoding (dependencia directa de aiortc, listada explicitamente)
opencv-python>=4.9.0       # OpenCV sin CUDA (CPU). Para GPU usa requirements-cuda.txt
numpy>=1.26.0

# Utilidades
python-dotenv>=1.0.0       # Carga variables de .env
